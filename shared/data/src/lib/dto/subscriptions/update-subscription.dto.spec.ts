import { UpdateSubscriptionDto } from './update-subscription.dto';

describe('UpdateSubscriptionDto', () => {
  it('should be defined', () => {
    expect(new UpdateSubscriptionDto()).toBeDefined();
  });
});
