import { UpdateDesignOptionsDto } from './update-design-options.dto';

describe('UpdateDesignOptionsDto', () => {
  it('should be defined', () => {
    expect(new UpdateDesignOptionsDto()).toBeDefined();
  });
});
